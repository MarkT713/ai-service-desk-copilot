services:
  copilot:
    build: .
    ports: ["127.0.0.1:8000:8000"]
    volumes: ["desk-data:/data"]
    read_only: true
    tmpfs: ["/tmp:uid=10001,gid=10001"]
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
volumes:
  desk-data:
